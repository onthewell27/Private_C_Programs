//任意の数を上限値として、ランダムに整数を出す関数

#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int main(void){

	int r ;
	
	const int num = 99 ; //上限数

	srand(time(NULL));

	r = (rand()%num) ;

	printf("今回の数は %d です。\n",r);

	return 0 ;

}
